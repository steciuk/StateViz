export enum SvelteBlockType {
	anchor = 'anchor',
	block = 'block',
	catch = 'catch',
	component = 'component',
	each = 'each',
	element = 'element',
	else = 'else',
	if = 'if',
	iteration = 'iteration',
	key = 'key',
	pending = 'pending',
	slot = 'slot',
	text = 'text',
	then = 'then',
}

